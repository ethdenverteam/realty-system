# Настройка LOGS_DOWNLOAD_TOKEN

## Быстрая настройка

### 1. На сервере

Добавьте в `.env` файл или `docker-compose.yml`:

```bash
LOGS_DOWNLOAD_TOKEN=your_secure_random_token_here
```

**Генерация безопасного токена:**
```bash
# Linux/Mac
openssl rand -hex 32

# Или Python
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

### 2. На локальном компьютере

Создайте файл `.api_token` в корне проекта:
```
your_secure_random_token_here
```

**Важно:** Используйте тот же токен, что и на сервере!

### 3. Перезапустите контейнеры

```bash
docker-compose down
docker-compose up -d
```

## Проверка

Запустите скрипт:
```bash
python download_logs.py https://your-domain.com
```

Если всё настроено правильно, логи скачаются в `logs_server/`.

## Безопасность

- ✅ Токен не истекает (в отличие от JWT)
- ✅ Используется только для скачивания логов
- ✅ Файл `.api_token` в `.gitignore` (не попадет в git)
- ✅ Можно использовать длинный случайный токен

## Пример настройки

**На сервере (`docker-compose.yml`):**
```yaml
services:
  web:
    environment:
      - LOGS_DOWNLOAD_TOKEN=abc123xyz789secure_token_here
```

**На локальном компьютере (`.api_token`):**
```
abc123xyz789secure_token_here
```

Готово! 🎉

