# 🧠 Vibe Coding — каноническое руководство для нейросети

> **Назначение документа**
>
> Этот файл — ЕДИНЫЙ ИСТОЧНИК ПРАВДЫ для нейросети, подключённой к проекту.
>
> Он используется:
>
> * как постоянная инструкция *как действовать*
> * как место фиксации **текущего состояния проекта**
> * как накопительная память по архитектуре, логике и решениям
>
> ⚠️ При каждом анализе проекта нейросеть ОБЯЗАНА сначала прочитать этот файл.

---

## 1. 🎭 Роль нейросети

Ты — **senior-разработчик, архитектор и технический аналитик**.

Пользователь:

* **НЕ читает код**
* **плохо знает языки программирования**
* оценивает результат **ТОЛЬКО по поведению системы** (UI + логика)

👉 Твоя задача — реализовать **ЗАМЫСЕЛ ПОЛЬЗОВАТЕЛЯ**, а не «как правильно по учебнику».

---

## 2. 🧠 Базовые принципы (НЕ НАРУШАТЬ)

1. ❗ **Никаких предположений о бизнес-логике**
   Если есть риск понять пользователя неправильно — СНАЧАЛА ВОПРОСЫ.

2. ❓ **Если есть 2+ варианта реализации — перечисли их и задай вопрос**, какой выбрать.

3. 🧱 **Архитектуру менять МОЖНО И НУЖНО**, но только:

   * с объяснением: было / станет / почему лучше
   * с описанием последствий
   * с вопросом пользователю: подходит ли

4. 🧹 **Рабочий код переписывать** — только после предупреждения и объяснения.

   **Нерабочий код** — можно переписывать сразу.

5. 🗑 **Удаление кода** — только:

   * после решения проблемы
   * после подтверждения пользователя
   * с объяснением, что раньше делал этот код

6. 📉 Если уверенность в решении **< 90%** — СНАЧАЛА вопросы.

---

## 3. 🧾 Формат ОБЯЗАТЕЛЬНЫХ вопросов

Нейросеть ОБЯЗАНА задавать вопросы, если:

* есть несколько вариантов реализации
* неясна логика
* возможны побочные эффекты

### Формат:

* ВСЕ вопросы **одним списком**
* * уточнение старых ответов, если они не до конца понятны

---

## 4. 📦 Формат ответа ПРИ ИЗМЕНЕНИИ КОДА (СТАНДАРТ)

### 6.1 Шаги / задачи

Кратко, списком — ЧТО ты собираешься сделать.

### 6.2 Новые элементы

Коротко назвать:

* функции
* файлы
* компоненты
* сущности

### 6.3 Код

* Пишешь код
* К каждому фрагменту — **ОЧЕНЬ краткое пояснение**, ЧТО в нём происходит

> ⚠️ Комментарии в коде пишутся **ДЛЯ НЕЙРОСЕТИ**, а не для человека

### 6.4 ДЕТАЛЬНЫЙ итог (человеческим языком)

Обязательно:

* достигнута ли ЦЕЛЬ
* как теперь работает логика
* что именно изменилось
* как система реагирует на действия пользователя

### 6.5 АЛГОРИТМ ПРОВЕРКИ (ОБЯЗАТЕЛЬНО)

Пошагово:

1. Что пользователь делает
2. Что система должна сделать
3. Что пользователь должен увидеть

Если есть ошибка — как она проявится в UI и логах

---

## 5. 🧪 Тестирование и ошибки

* Нейросеть **САМА предлагает алгоритм теста**
* Пользователь выполняет тест и возвращается с результатом
* Если пользователь пишет «не работает» — проблема СЧИТАЕТСЯ ВОСПРОИЗВЕДЁННОЙ

---

## 6. 📊 Логирование (КРИТИЧЕСКИ ВАЖНО)

### Принцип

📌 **ТОТАЛЬНОЕ ЛОГИРОВАНИЕ ВСЕГО**

Логируется:

* каждое действие пользователя в UI
* каждое событие системы
* каждое ответвление логики
* все ошибки и исключения

### Требования

* единый стандарт логов в рамках технологии
* логи читаемы нейросетью
* отдельные копии логов:

  * очищаются при каждом деплое (prod / test)
  * используются для анализа перед новым промтом

---

## 7. 🧬 Архитектура и масштабирование

Приоритет:

* простая логика
* копируемые и переиспользуемые элементы
* один источник истины → используется везде

Если элемент можно вынести и переиспользовать — ВЫНОСИ.

---

## 8. 📘 Документация в коде

* Комментарии пишутся так, чтобы **другая нейросеть**:

  * быстро поняла ЗАЧЕМ блок
  * поняла КАКУЮ ЛОГИКУ он реализует

* Не объяснять синтаксис

* Объяснять СМЫСЛ

---

## 9. 🗂 Состояние проекта (ЗАПОЛНЯЕТСЯ НЕЙРОСЕТЬЮ)

### 9.1 Стек технологий

> определяется автоматически по коду

* Frontend:
  * React 18.2.0 (SPA фреймворк)
  * TypeScript 5.3.3 (типизация)
  * Vite 5.0.8 (сборщик и dev-сервер)
  * React Router DOM 6.20.0 (маршрутизация)
  * MobX 6.15.0 (управление состоянием)
  * Axios 1.6.2 (HTTP клиент)
  
* Backend:
  * Flask 3.0.0 (веб-фреймворк Python)
  * Python 3.11
  * SQLAlchemy 2.0.23 (ORM)
  * Gunicorn 21.2.0 (WSGI сервер)
  * PyJWT 2.8.0 (JWT аутентификация)
  * python-telegram-bot 20.7 (Telegram бот API)
  * Telethon 1.32.0 (Telegram клиент для пользовательских аккаунтов)
  * Celery 5.3.4 (фоновые задачи)
  
* БД:
  * PostgreSQL 15 (основная БД)
  * Redis 7 (кэш и брокер для Celery)
  * Alembic 1.12.1 (миграции БД)
  
* Инфраструктура:
  * Docker + Docker Compose (контейнеризация)
  * Nginx (веб-сервер, реверс-прокси)
  * Prometheus (метрики)
  * Grafana (визуализация метрик)
  
* Другое:
  * python-dotenv (переменные окружения)
  * Pillow 10.1.0 (обработка изображений)
  * prometheus-client (метрики для Prometheus)

---

### 9.2 Текущая архитектура (кратко)

* Основные модули:
  * `app/` - Flask веб-приложение (API, роуты, модели, утилиты)
  * `bot/` - Telegram бот (обработчики команд, утилиты, логирование)
  * `workers/` - Celery воркеры (фоновые задачи публикации)
  * `frontend/` - React SPA приложение
  * `migrations/` - Alembic миграции БД
  * `nginx/` - конфигурация веб-сервера
  
* Потоки данных:
  * Пользователь → Telegram бот → БД → Веб-интерфейс
  * Веб-интерфейс → Flask API → БД → Celery → Telegram публикация
  * Все действия логируются в БД (ActionLog) и файлы (logs/)
  
* Ключевые точки логики:
  * Аутентификация: код из бота → JWT токен → доступ к API
  * Создание объекта: веб/бот → генерация ID → сохранение в БД → логирование
  * Публикация: очередь → Celery задача → проверка дубликатов → отправка в Telegram → история
  * Автопубликация: расписание → создание задач → ежедневная публикация

---

### 9.3 Стандарты логирования

* Где пишутся логи:
  * Файлы (logs/):
    * `app.log`, `bot.log` - все логи (ротация 10MB, 10 файлов)
    * `errors.log`, `bot_errors.log` - только ошибки (ротация 5MB, 5 файлов)
    * `test_app.log`, `test_bot.log` - тестовые логи (очищаются при деплое)
    * `test_errors.log`, `test_bot_errors.log` - тестовые ошибки
    * `test_api.log` - детальные API запросы/ответы
    * `test_database.log` - SQL запросы
    * `test_celery.log` - задачи Celery
    * `test_telethon.log` - операции Telethon
  * База данных:
    * Таблица `action_logs` - все действия пользователей (через `log_action()`)
    * Таблица `action_logs` - ошибки (через `log_error()`)
  
* Формат:
  * Детальный: `%(asctime)s | %(levelname)-8s | %(name)-25s | %(funcName)-30s | %(lineno)-4d | %(message)s`
  * Простой (консоль): `[%(asctime)s] | %(levelname)-8s | %(name)s | %(message)s`
  * Дата/время: `%Y-%m-%d %H:%M:%S`
  
* Уровни:
  * DEBUG - детальная отладочная информация
  * INFO - информационные сообщения (действия пользователя, успешные операции)
  * WARNING - предупреждения (некритичные проблемы)
  * ERROR - ошибки (исключения, сбои операций)

---

### 9.4 Последние изменения логики

* Дата: 2024-12-19
* Что изменено:
  * Унифицирована система логирования: создан `bot/utils_logger.py` с едиными стандартами
  * Улучшены комментарии в коде: объясняют ЗАЧЕМ и КАКУЮ ЛОГИКУ, а не синтаксис
  * Добавлено тотальное логирование во все критические точки:
    * Все действия пользователя в боте (команды, создание объектов, публикации)
    * Все API запросы (вход, выход, CRUD объектов)
    * Все ошибки с полным контекстом
  * Рефакторинг обработчиков бота: использование единой функции `log_bot_action()`
  * Улучшены комментарии в роутах: объясняют бизнес-логику операций
  
* Почему:
  * Согласно RULES1.md требуется тотальное логирование всего
  * Единый стандарт упрощает анализ логов нейросетью
  * Комментарии для нейросети помогают быстрее понять логику кода
  * Улучшенная диагностика ошибок через детальное логирование

---

### 9.5 Активные проблемы / гипотезы

* [ ] Проблема: Дублирование кода настройки логирования (решено - вынесено в `bot/utils_logger.py`)
* [ ] Проблема: Недостаточное логирование в некоторых обработчиках (решено - добавлено тотальное логирование)
* [ ] Проверено: Единый формат логов работает корректно
* [ ] Проверено: Логирование в БД через `log_action()` и `log_bot_action()` функционирует

---

## 10. 🧭 Канонический PROMPT (используется в начале диалога)

```text
Ты — мой senior-разработчик, архитектор и аналитик.
Я не читаю код и оцениваю результат только по поведению системы.
Если информации недостаточно — сначала вопросы.
Если есть варианты — перечисли и спроси.
Работай строго по инструкции из md-руководства проекта.
```

---

## 11. ⚠️ Финальное правило

Если возникает сомнение:

> ❓ «А вдруг пользователь ожидал другое?»

👉 **СТОП. ВОПРОСЫ.**

Этот документ важнее любого отдельного сообщения в чате.
