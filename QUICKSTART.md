# Быстрый старт React приложения

## 🚀 Установка и запуск

### 1. Установите зависимости frontend

```bash
cd frontend
npm install
```

### 2. Запустите dev-сервер (для разработки)

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:3000` с hot-reload.

### 3. Соберите для продакшена

```bash
npm run build
```

Собранные файлы появятся в папке `static/` в корне проекта.

### 4. Запустите Flask сервер

```bash
# Flask будет отдавать статику из static/ и API из /system/*
python -m flask run
# или
gunicorn app:app
```

## 📱 Что изменилось

✅ **Полноценное React SPA** вместо HTML страниц  
✅ **Mobile-first дизайн** в стиле luma.com  
✅ **Современный UI** - минималистичный и чистый  
✅ **Все страницы в формате приложения** - без перезагрузок  
✅ **Единая навигация** - нижняя панель на мобильных  

## 🎨 Дизайн

Приложение использует минималистичный дизайн:
- Чистые линии и много белого пространства
- Минималистичная цветовая палитра (черный, белый, серый)
- Плавные переходы
- Mobile-first подход

## 📂 Структура

```
frontend/
├── src/
│   ├── pages/          # Все страницы
│   ├── components/     # Переиспользуемые компоненты
│   ├── contexts/       # React Contexts (Auth)
│   └── utils/          # API и утилиты
```

## 🔗 Роуты

- `/login` - Вход
- `/admin/dashboard` - Админ панель
- `/admin/dashboard/bot-chats` - Управление чатами
- `/admin/dashboard/logs` - Логи
- `/user/dashboard` - Пользовательская панель
- `/user/dashboard/objects` - Объекты
- `/user/dashboard/objects/create` - Создание объекта

Все API запросы идут через `/system/*` (настроено автоматически).

